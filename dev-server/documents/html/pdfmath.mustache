<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>PDF math quote render test</title>
    <!-- Claim to be the arXiv PDF, so the client loads a PDF-style (urn/page-selector)
         annotation and AnnotationQuote runs its PDF OCR path. -->
    <link rel="canonical" href="https://arxiv.org/pdf/2312.03638" />
  </head>
  <body style="font: 18px/1.7 Georgia, serif; max-width: 40em; margin: 3em auto">
    <h1>PDF math quote render test</h1>
    <p>
      A PDF-style annotation (page selector + flattened math quote) on the residue equation
      of this document should OCR at display time and render <em>\(\omega=\operatorname{Res}\,\ldots\)</em>
      in the sidebar quote — not the garbled text-layer capture.
    </p>
    <script class="js-hypothesis-config" type="application/json">
      {"assetRoot": "http://localhost:3011/hypothesis/1.0.0-dummy-version/", "sidebarAppUrl": "http://localhost:3012/fork-app.html", "apiUrl": "http://localhost:5000/api/", "authDomain": "localhost", "openSidebar": true}
    </script>
    {{{ hypothesisScript }}}
  </body>
</html>
